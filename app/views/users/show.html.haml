%h2 
  = current_user.firstname + ' ' + current_user.lastname
  
%p
%b First name:
= current_user.firstname

%p
%b Last name:
= current_user.lastname

%p
%b Email address:
%a{href: 'mailto:' + current_user.email}
  = current_user.email

%br
%br

%h4 Your latest reviews:
.row
  .col-md-6
    - if reviews.count == 0
      %p
        %i No reviews yet.
    - else
      %ul.list-group
      = render reviews.sort_by{|t| - t.created_at.to_i}.first(5)

= link_to 'Edit profile', edit_user_registration_path, class: 'btn btn-default'